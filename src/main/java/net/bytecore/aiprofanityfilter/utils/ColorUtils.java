package net.bytecore.aiprofanityfilter.utils;



import java.awt.*;

public class ColorUtils {

    /*public static @NotNull String colorize(final @NotNull String message) {
        return ChatColor.translateAlternateColorCodes('&', message);
    }

    public static List<String> colorize(List<String> message) {
        for (int i = 0; i < message.size(); i++) {
            message.set(i, colorize(message.get(i)));
        }
        return message;
    }*/

}
